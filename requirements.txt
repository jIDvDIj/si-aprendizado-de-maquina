# Dependências do projeto "Previsão de AVC" (testado com Python 3.12)
#
# Instalação:  pip install -r requirements.txt
#
# Obs.: o pandas fica na série 2.x de propósito — o pandas 3.0 (novo dtype de
# string com backend pyarrow) ainda apresenta incompatibilidades com o
# Streamlit; a série 2.x é também a distribuída no Google Colab.
pandas>=2.0,<3.0
numpy>=1.26
scikit-learn>=1.3
imbalanced-learn>=0.11
matplotlib>=3.7
seaborn>=0.12
streamlit>=1.30
joblib>=1.3
